# KDBP Configuration — populated by kdbp-init or manually
project_name: "{{project_name}}"
user_name: "{{user_name}}"
user_skill_level: "{{user_skill_level}}"
communication_language: "{{communication_language}}"
document_output_language: "{{document_output_language}}"
output_folder: "{project-root}/_kdbp-output"
planning_artifacts: "{project-root}/_kdbp-output/planning-artifacts"
implementation_artifacts: "{project-root}/_kdbp-output/implementation-artifacts"
project_knowledge: "{project-root}/docs"
